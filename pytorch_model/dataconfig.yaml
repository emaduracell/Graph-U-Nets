data:
  include_mesh_pos: False
  normalization_method: "standard" # 'standard' or 'row'
  max_trajs: 10
  tfrecord_path: "raw_data/train.tfrecord"
  meta_path: "raw_data/meta.json"
  output_dir: "processed_data/data_world=None_mesh=False_norm=standard_At=True" # Change this accordingly to your other parameter in the config
  add_world_edges: "None" # 'neighbours' or 'radius' or 'None'
  radius_world_edge: 0.03
  k_neighb: 1
  a_time_var: False